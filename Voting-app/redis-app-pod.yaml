apiVersion: v1
kind: Pods
metadata:
  name: redis-pod
  app: demo-voting-app
  labels:
    name: redis-pod
    app: demo-voting-app
spec:
  containers:
    - name: redis-pod
      image : redis
      ports:
        - containerPort: 6379